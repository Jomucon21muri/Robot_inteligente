document.addEventListener('DOMContentLoaded', () => {
    console.log('Museo Virtual Initialized');

    // Future functionality: Add simple interactions here
    // e.g. Animate elements on scroll, handle modal popups for quick info

    const cards = document.querySelectorAll('.zone-card');

    // Add a simple stagger effect or intersection observer if needed later
    // For now, the CSS animations handle the hover and float effects
});
